// TODO: 因为mock所以屏蔽业务实现
export default {
  // posts: () => posts,
  // authors: () => authors,
  // author: (_, {id}) => find(authors, {id}),
  // searchInterface: (_, {text}) => {
  //   if (text === 'notice') {
  //     return notices[0]
  //   } else {
  //     return reminds[0]
  //   }
  // },
  // searchUnion: (_, {text}) => {
  //   if (text === 'notice') {
  //     return notices[0]
  //   } else {
  //     return reminds[0]
  //   }
  // }
}
